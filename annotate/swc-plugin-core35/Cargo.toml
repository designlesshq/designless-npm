# GENERATED by gen-abi-targets.mjs - do not hand-edit.
#
# ABI target: swc_core 35. Known hosts: Next 15.5.x (swc_plugin_runner 18).
#
# The SOURCE is shared with ../swc-plugin - one lib.rs, one behaviour, two ABI
# pins. The only construct the cores disagree on is a single JSX attribute
# value, behind the `swc_jsx_attr_str` cfg that build.rs sets per target.
[package]
name = "designless-annotate-swc-core35"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "SWC plugin (swc_core 35 ABI): stamp annotate/v1 source markers on host JSX elements."
publish = false
build = "build.rs"

[lib]
crate-type = ["cdylib"]
path = "../swc-plugin/src/lib.rs"

[dependencies]
swc_core = { version = "35", features = ["ecma_plugin_transform", "ecma_ast", "ecma_visit", "common"] }
swc_plugin_macro = "=1.1.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = "z"
lto = true
strip = true
